apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hades-clone-container.fullname" . }}-test-connection"
  labels:
    {{- include "hades-clone-container.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hades-clone-container.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
