apiVersion: v1
kind: Service
metadata:
  name: {{ include "hades-api.fullname" . }}
  labels:
    {{- include "hades-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hades-api.selectorLabels" . | nindent 4 }}
