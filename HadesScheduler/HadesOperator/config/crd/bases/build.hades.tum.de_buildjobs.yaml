---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: buildjobs.build.hades.tum.de
spec:
  group: build.hades.tum.de
  names:
    kind: BuildJob
    listKind: BuildJobList
    plural: buildjobs
    singular: buildjob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BuildJob is the Schema for the buildjobs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              maxRetries:
                description: (Optional) Number of times to retry the job on failure
                format: int32
                minimum: 0
                type: integer
              metadata:
                additionalProperties:
                  type: string
                description: Additional metadata (e.g., course ID, submitter, language,
                  etc.)
                type: object
              name:
                description: Job name, useful for debugging
                type: string
              steps:
                description: Build steps to be executed sequentially
                items:
                  properties:
                    cpuLimit:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Resource limits (if empty, defaults will be used
                        from LimitRange/Quota)
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    id:
                      description: Step ID (must be unique and start from 1)
                      format: int32
                      minimum: 1
                      type: integer
                    image:
                      description: Execution image (required)
                      minLength: 1
                      type: string
                    memoryLimit:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    metadata:
                      additionalProperties:
                        type: string
                      description: Additional metadata like environment variables,
                        credentials, etc.
                      type: object
                    name:
                      description: Step name
                      type: string
                    script:
                      description: Script or command to run inside the container
                      type: string
                  required:
                  - id
                  - image
                  type: object
                minItems: 1
                type: array
              timeoutSeconds:
                description: (Optional) Timeout for the whole job; if exceeded, the
                  job is marked as failed
                format: int64
                minimum: 1
                type: integer
            required:
            - steps
            type: object
          status:
            properties:
              completionTime:
                format: date-time
                type: string
              currentStep:
                description: ID of the currently executing step
                format: int32
                type: integer
              message:
                type: string
              phase:
                description: Phase of the BuildJob lifecycle
                type: string
                enum:
                  - Pending
                  - Running
                  - Succeeded
                  - Failed
              podName:
                description: Name of the Pod/Job created by the Operator, helpful
                  for troubleshooting
                type: string
              retryCount:
                description: Number of retry attempts so far
                format: int32
                type: integer
              startTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
