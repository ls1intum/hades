apiVersion: v1
kind: Service
metadata:
  name: {{ include "hades-scheduler.fullname" . }}
  labels:
    {{- include "hades-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hades-scheduler.selectorLabels" . | nindent 4 }}
