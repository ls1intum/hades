apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hades-scheduler.fullname" . }}-test-connection"
  labels:
    {{- include "hades-scheduler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hades-scheduler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
